import { motion } from "framer-motion";
import { Braces, Database, Globe, Network } from "lucide-react";
const services=[{title:"Systems & C++",description:"Concurrent C++ applications with TCP sockets, TLS/OpenSSL, threads, mutex synchronization, graph logic and file-backed persistence.",icon:Braces},{title:"Machine Learning",description:"TensorFlow/Keras models, feature engineering, preprocessing, LSTM forecasting, and physics-informed learning with quantitative evaluation.",icon:Database},{title:"Scientific Computing",description:"Translate mathematical constraints into trainable models using automatic differentiation, numerical objectives and optimization strategies.",icon:Globe},{title:"Networking & Security",description:"TCP/IP communication, encrypted connections, multi-client architecture and synchronization for reliable networked applications.",icon:Network}]
const updateGlow=(e:React.MouseEvent<HTMLDivElement>)=>{const el=e.currentTarget,r=el.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top;el.style.setProperty("--mouse-x",`${x}px`);el.style.setProperty("--mouse-y",`${y}px`);el.style.setProperty("--rotate-x",`${((y/r.height)-.5)*-5}deg`);el.style.setProperty("--rotate-y",`${((x/r.width)-.5)*5}deg`)};
const resetGlow=(e:React.MouseEvent<HTMLDivElement>)=>{e.currentTarget.style.setProperty("--rotate-x","0deg");e.currentTarget.style.setProperty("--rotate-y","0deg")};
export const ServicesSection=()=> <section id="services" className="max-w-7xl mx-auto px-6 py-24">
<motion.div initial={{opacity:0,y:30}} whileInView={{opacity:1,y:0}} viewport={{once:true}} className="mb-14 text-center"><h2 className="text-3xl md:text-5xl font-bold tracking-tight mb-4">What I <span className="text-gradient-primary">Build</span></h2><p className="text-muted-foreground max-w-2xl mx-auto">The work is centered on problems where implementation details matter: concurrency, network communication, numerical constraints and model evaluation.</p></motion.div>
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-5">{services.map((service,i)=>{const Icon=service.icon;return <motion.div key={service.title} initial={{opacity:0,y:25}} whileInView={{opacity:1,y:0}} transition={{delay:i*.08}} viewport={{once:true}} onMouseMove={updateGlow} onMouseLeave={resetGlow} className="premium-hover-card service-card glass-panel rounded-3xl p-6 border border-foreground/10"><div className="service-icon w-12 h-12 rounded-2xl bg-primary/10 flex items-center justify-center text-primary mb-5"><Icon className="w-6 h-6"/></div><h3 className="font-bold text-lg mb-3">{service.title}</h3><p className="text-sm text-muted-foreground leading-relaxed">{service.description}</p><span className="service-arrow">↗</span></motion.div>})}</div>
</section>;